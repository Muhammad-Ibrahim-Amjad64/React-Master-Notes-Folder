// Filter method :
//          It will return a new array on some specific conditions 

// Concat method : 
//          It will add the item (at the end of the array)without effecting the current array 

const existingCartItemIndex=state.cartItems.findIndex((item)=>item.id===action.id)